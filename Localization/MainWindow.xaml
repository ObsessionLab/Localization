<Window x:Class="Localization.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Localization"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="{Binding HelloText}" FontSize="16" Margin="10"/>
            <TextBlock Text="{Binding String1}" FontSize="16" Margin="10"/>
            <Button Content="{Binding ChangeLanguageText}" Command="{Binding ChangeLanguageCommand}" Width="150">
                <Button.ToolTip>
                    <ToolTip Content="{Binding ChangeLanguageText}"/>
                </Button.ToolTip>
            </Button>
        </StackPanel>
    </Grid>
</Window>